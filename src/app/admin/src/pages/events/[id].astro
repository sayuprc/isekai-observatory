---
import { DetailView } from '../../components/event/EditableForm';
import Layout from '../../layouts/Layout.astro';
const { id } = Astro.params;
---
<Layout pageTitle="イベント詳細"><DetailView eventId={id ?? ''} client:only="solid-js" /></Layout>
